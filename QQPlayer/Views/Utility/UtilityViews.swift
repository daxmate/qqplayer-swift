//  UtilityViews.swift
//  QQPlayer
//
//  本文件原有的 InitializationView 于 2026-09-12 审计 BATch B5 删除：
//  `grep -rn "InitializationView"` 全仓零引用（含 Share / PlayerWidget / SiriIntentsExtension），
//  且其消费的 Localized.foundTracks / percentComplete / waitingColon / andMore / processingColon
//  只在死路径上使用。索引进度 UI 的现役实现是 LibraryView / WhatsNewView 等。
//
//  文件保留为空占位（审计红线：不删文件）。
