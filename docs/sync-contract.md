# 局域网同步契约（L0：逐实体承诺与规则）

> **状态**：2026-09-15 与用户逐条拍板冻结。
> **地位**：本文件是同步的**需求真相**——实现逐行兑现，验收逐行打勾。与它冲突的代码/文档一律作废。
> **配套**：`docs/lan-sync-design.md`（协议与实现设计）、`docs/sync-matrix.md`（实体 × 能力矩阵）、`docs/sync-invariants.md`（不变量清单）。
> **入库方式**：`docs/` 在 `.gitignore`（本地产物不入库），本文件属**强制入库**的规格文件 → `git add -f`。

## 0. 怎么用这份文件

1. 每个实体六列：**用户可见承诺 / 权威端与冲突 / 删除 / 触发 / 失败披露 / 身份键**。缺一列不算定义。
2. **「不承诺」也是承诺**：本文件写明"不做"的，任何人不得实现它——防止再出现"承诺了做不到"或"没承诺却偷偷做"。
3. 修改流程：**改本文件 → 用户确认 → 才动代码**。禁止用追加章节"覆盖"前面的定义（历史教训：§12b 覆盖 §6）。
4. 验收从本文件生成，不从"发现的 bug"生成。

---

## 1. 数据面（走变更日志通道，帧 8/9）

### A 收藏（favorite）

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | 同一首歌在两端都显示"已收藏" |
| 权威端与冲突 | 对等；**无时间序**（删除不传播 ⇒ 只存在"加"，不存在冲突） |
| 删除 | **不传播** |
| 触发 | 连接就绪后自动一轮 + 面板手动「同步数据」 |
| 失败披露 | 已应用 / 未定位 / 缺依赖 / 身份歧义 —— **逐实体计数上屏** |
| 身份键 | ① `content_hash`（跨端，**优先**）；② 曲库相对路径（**第二身份**：指纹缺失时用） |

### B 播放历史（playHistory）

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | 一端听过的歌，另一端「最近播放 / 常听排行」出现 |
| 权威端与冲突 | 对等；**LWW by `played_at`** |
| 删除 | 不传播 |
| 触发 | 同 A |
| 失败披露 | 同 A |
| 身份键 | 同 A（`content_hash` 优先 → 曲库相对路径兑底） |

### C 歌单结构（playlist：名称 / 结构）

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | 一端建的歌单（名称、结构）在另一端存在 |
| 权威端与冲突 | 对等；**LWW by `updatedAt`** |
| 删除 | 不传播 |
| 触发 | 同 A |
| 失败披露 | **歌单级失败必须单独计数上屏**（当前缺） |
| 身份键 | 不引用歌曲（歌单标识 `slug`），不得被判「未定位」 |

### D 歌单成员（playlistItem）

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | 歌单内容两端一致（限两端都有的歌） |
| 权威端与冲突 | 对等；rowKey = `slug\|trackStableId`；**顺序以发起端（Mac）为准**，对端只补不重排 |
| 删除 | 不传播 |
| 触发 | 同 A |
| 失败披露 | 本地缺歌 → **挂起**，歌到位后重放（已实现）；身份歧义 → 不落库 + 计数披露 |
| 身份键 | 同 A（`content_hash` 优先 → 曲库相对路径兑底） |

---

## 2. 内容面

### E 跨端续播（playbackPosition）

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | **默认关**。开关开启后，一端"上次听到哪"可在另一端续播 |
| 权威端与冲突 | 对等；**LWW by `updatedAtMs`** |
| 删除 | 不传播 |
| 触发 | 开关开 + 播放状态保存点 |
| 失败披露 | 关时收到 → 「未启用」单独计数（已实现） |
| 身份键 | 同 A（`content_hash` 优先 → 曲库相对路径兑底） |

### F1 网络下载歌词

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | **不承诺跨端**（用户 2026-09-15 重申） |
| 理由 | 重新下载成本极低、数据量小、单首播放时按需获取 |
| 其余列 | —（不参与同步：无权威端、无冲突、不触发、不披露） |

### F2 对齐歌词（本地对齐产物）

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | 一端对齐好的歌词，随歌到达另一端 |
| 权威端与冲突 | **只补不覆盖**（对端已有对齐结果则保留对端的） |
| 删除 | 不传播 |
| 触发 | 随歌传输（内容通道）+ 补发通道（当前缺） |
| 失败披露 | 歌词丢弃必须计数上屏（当前缺） |
| 身份键 | 歌曲 `content_hash`（命名空间 `@lyrics/{content_hash}.json`） |

### G 曲库音频文件

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | 勾选同步集合里的歌，在另一端**可播** |
| 权威端与冲突 | **内容权威在 Mac**：同一相对路径、内容不同（`content_hash` 不同）时，**Mac 的内容覆盖移动端**；唯一例外 = 用户显式发起「从设备取回」 |
| 判定与执行细则 | ① 判定用 `content_hash`，**不用 mtime / 大小** ② 覆盖必须**计数上屏**（"替换 N 首"），不静默 ③ **原子替换**：写临时文件 → 校验 → 再删旧文件 ④ 路径不变 ⇒ 端内身份 `stableId` 不变 ⇒ 收藏 / 历史 / 歌单引用安全 |
| 删除 | **绝不跨端删除文件**。且——**"路径消失"（不删）与"路径存在但内容变"（可覆盖）是两条独立规则，互不替代** |
| 触发 | 集合 = 用户显式选择（空选择集 = 不推不拉）；连接就绪后自动 + 面板手动 |
| 失败披露 | 传输失败可查（已有 report） |
| 身份键 | 相对路径（跨端统一）+ `content_hash` |

> **已知设计行为（非 bug）**：移动端若自行重新导入同名文件（文件 App 导入到同一相对路径），Mac 下次推送会覆盖它——移动端为纯被动端，面板需如实提示。

### H 歌单自定义封面

| 列 | 内容 |
| --- | --- |
| 用户可见承诺 | **不承诺跨端**（用户 2026-09-15） |
| 理由 | 封面由歌单内歌曲封面**自动合成** = 派生数据，两端各自合成，无可同步之物 |
| 防回归 | `custom_cover_image_path` 之类**设备本地路径永远不得作为跨端值传输**（历史事故：对端必然加载不出） |

---

## 3. 全实体通用规则

1. **删除一律不传播**（用户 2026-09-10：「删除是自己的事情」）。
2. **触发**：连接就绪后自动跑一轮（补发不依赖用户动作）+ 面板手动可再跑。
3. **失败必须分类计数并上屏**，禁止静默；「已应用 N」必须等于**真落库行数**（丢弃、跳过、歧义不算应用）。
4. **身份**：跨端恒用 `content_hash`；端内 `stableId` 只在本端使用，**永不上线**。
   - `content_hash` 缺失时，以**曲库相对路径为第二身份**（唯一命中才落库；歧义不落库并计数；
     本地无此歌则按相对路径挂起）。相对路径是两端统一的跳端键（Mac 推送文件时就是按相对路径
     落到 iOS 曲库）。解析顺序 = `content_hash` 优先，**存在指纹时绝不看相对路径**。
   - 两把键都拿不到 = 真空缺 → 不落库、不挂起，**计数上屏**。
5. **业务数据只走数据通道**；内容通道只搬字节，不改业务状态。
6. **移动端纯被动**（不主动同步、不主动上传）；发起方为桌面端。

---

## 4. 变更记录

| 日期 | 变更 | 依据 |
| --- | --- | --- |
| 2026-09-18 | **身份兑底：曲库相对路径当第二身份**（A/B/D/E 的身份键列）；发送侧指纹缺失时 wire 带 `relativePath`（加性可选字段）；接收侧判定全在身份入口内部（内容指纹优先 → 相对路径唯一命中 → 歧义不落库 → 挂起）；新增「身份歧义 N 条」披露（>0 才显示，5 语） | 用户 2026-09-15 拍板：相对路径当第二身份 |
| 2026-09-15 | 首次冻结：歌词拆成「网络下载（不承诺）/ 对齐歌词（只补不覆盖）」；歌单封面改为"自动合成 ⇒ 不承诺"；文件覆盖规则定为「内容权威在 Mac」（取代"发起端覆盖目的端"的措辞）；歌单项顺序以发起端为准 | 用户逐条拍板（08:49–08:50） |
